.env
.env.*
!.env.example
*.log
*.tmp
.DS_Store
Thumbs.db
__pycache__/
*.py[cod]

# Local exports and downloaded source documents should stay out of the operating layer by default.
exports/
downloads/
local-data-room-copy/

# Local secrets, credentials, and sensitive exports.
secrets/
credentials/
*.pem
*.key
*.pfx
*.ppk

# Legacy private-memory pattern. New employee records belong in ignored instance/runtime/.
# Root employees.json is an empty template, not a live roster.
agents/employees/*/memory.private.md

# Company evidence and authentication must never enter Git.
instance/business.yaml
instance/company.json
instance/employees.json
instance/runtime/
instance/private/
.venv/
*.sqlite3
*.sqlite3-*
